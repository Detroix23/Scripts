# Custom scripts

## Repository.
Divided scripts between:
- `linux`;
- `windows`.

## General instructions.
For lighter scripts, we use `alias`.

Using shell scripts.
	- Need to `chmod` in order for them to be allow to be executed.
	- We put the script under `/usr/local/bin`.


